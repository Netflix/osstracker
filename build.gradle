buildscript {
    repositories {
        jcenter()
    }
}

plugins {
    id 'nebula.netflixoss' version '3.3.0'
}

subprojects {
    apply plugin: 'nebula.netflixoss'
    group = 'com.netflix.oss.tools'

    repositories {
        jcenter()
    }
}
